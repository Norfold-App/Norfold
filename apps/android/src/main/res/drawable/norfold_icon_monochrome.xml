<?xml version="1.0" encoding="utf-8"?>
<!-- Android 13+ themed-icon layer: the system tints via the glyph's alpha,
     so this reuses the raster foreground (white Norfold glyph on alpha). -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/norfold_icon_foreground"
    android:gravity="center" />
